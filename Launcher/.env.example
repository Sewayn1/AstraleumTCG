# Cloudflare R2 — credentials pour l'upload de gamebuild/ (release jeu)
# Copier ce fichier en ".env" et remplir les valeurs réelles.
# .env est dans .gitignore — ne JAMAIS le committer.

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
